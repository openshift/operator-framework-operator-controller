apiVersion: v1
kind: ConfigMap
metadata:
  name: httpd-script
data:
  httpd.sh: |
    #!/bin/sh
    echo "Version 1.2.0"
    mkdir -p /tmp/www
    echo true > /tmp/www/started
    echo true > /tmp/www/ready
    echo true > /tmp/www/live
    exec httpd -f -h /tmp/www -p 8081
